export function CFCPageHeader() {
  return (
    <header className="cfc-header">
      <h1 className="cfc-title">Create Coursework Flashcard</h1>
      <p className="cfc-subtitle">
        Add one entry at a time, move through your question-note pairs, then generate the full
        flashcard set.
      </p>
    </header>
  );
}
